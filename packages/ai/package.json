{
  "name": "@behindthestory/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "`./models` is the pricing catalogue and imports nothing — the studio bundles it for the model picker and the cost hints, so it must not reach the database.",
  "exports": {
    ".": "./src/index.ts",
    "./models": "./src/models.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@behindthestory/db": "workspace:*",
    "ai": "^7.0.59"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
